<p id="notice"><%= notice %></p>

<h1>Ranks</h1>

<table>
  <thead>
    <tr>
      <th>Rank</th>
      <th>Description</th>
      <th>Completed</th>
      <th>Advancement</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ranks.each do |rank| %>
      <tr>
        <td><%= rank.name %></td>
        <td><%= rank.description %></td>
        <td><%= rank.completed %></td>
        <td><%= rank.advancement_id %></td>
        <td><%= link_to 'Show', rank %></td>
        <td><%= link_to 'Edit', edit_rank_path(rank) %></td>
        <td><%= link_to 'Destroy', rank, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rank', new_rank_path %>
