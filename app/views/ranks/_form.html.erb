<%= form_for(rank) do |f| %>
  <% if rank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rank.errors.count, "error") %> prohibited this rank from being saved:</h2>

      <ul>
      <% rank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rank %>
    <%= f.text_field :rank %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :completed %>
    <%= f.date_select :completed %>
  </div>

  <div class="field">
    <%= f.label :advancement_id %>
    <%= f.number_field :advancement_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
